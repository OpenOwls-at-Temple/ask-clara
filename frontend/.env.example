VITE_API_BASE_URL=/api
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Local dev only — must match the backend's TEST_LOGIN_SECRET for the
# "Test Login" form on the sign-in page (dev builds only).
VITE_TEST_LOGIN_SECRET=e2e-local-secret
